#ifndef MATH_UTILS_H // Делаем проверку на наличие
#define MATH_UTILS_H // Создаем MATH_UTILS_H

int multiply(int a, int b);
double divide(double a, double b);
int substract(int a, int b);

#endif //MATH_UTILS_H